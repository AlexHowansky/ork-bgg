#!/usr/bin/env php
<?php

require_once realpath(__DIR__ . '/../vendor/autoload.php');

if ($argc !== 2) {
    printf("Usage: %s <username>\n", $argv[0]);
    exit;
}

(new \Ork\Bgg\Collection())->sync($argv[1]);
